import { makeStyles } from "@mui/styles";

const userStyle = makeStyles({
  content: {
    backgroundColor: "red",
    color: "#fff",
  },

  title: {
    fontSize: 50,
  },
});

export default userStyle;
